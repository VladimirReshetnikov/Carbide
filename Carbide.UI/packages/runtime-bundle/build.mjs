// @carbide-ui/avalonia-runtime-bundle build script — UI-M0 stub.
//
// At UI-M2 this script will:
//   1. Shell out to `dotnet publish -c Release ../runner-dotnet/`.
//   2. Copy the resulting AppBundle/_framework/ tree into ./_framework/.
//   3. Write bundle-manifest.json recording the triple-pin (Avalonia, Carbide, .NET)
//      and a per-file SHA256 roster.
// See carbide-ui-avalonia-approach-b-plan §6 for the full shape.

console.log("[runtime-bundle] UI-M0 stub. UI-M2 will implement this.");
